import { createTheme } from '@material-ui/core';

export const theme = createTheme({
  palette: {
    dark: {
      bcg: '#282c35',
      font: '#fff',
      primary: '#ffa7c4',
    },
    light: {
      bcg: '#fff',
      font: '#282c35',
      primary: '#d23669',
    },
  },
});
